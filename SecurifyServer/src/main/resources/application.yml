spring:
  sql:
    init:
      mode: always
  application:
    name: SecurifyServer
  mustache:
    suffix: .html
  datasource:
    url: jdbc:postgresql://localhost:5550/securifydb
    username: davidmisael
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        globally_quoted_identifiers: true
    show-sql: true